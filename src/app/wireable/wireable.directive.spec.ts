import { WireableDirective } from './wireable.directive';

describe('WireableDirective', () => {
  it('should create an instance', () => {
    const directive = new WireableDirective();
    expect(directive).toBeTruthy();
  });
});
